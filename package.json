{
  "name": "proyecto_privada",
  "version": "1.0.0",
  "type": "module",
  "description": "Privada",
  "main": "server.cjs",
  "scripts": {
    "start": "serve src/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve src/",
    "prettier": "prettier --write src/**/*.js",
    "prettier:check": "prettier --check src/**/*.js"
  },
  "author": "carlos",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "prettier": "^2.8.7",
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
